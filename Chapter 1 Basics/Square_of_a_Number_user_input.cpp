#include <iostream>
using namespace std;
int main () {
    cout<<"Enter the value of the number which is to be squared ";
    int x;
        cin>>x; // We use forward slash in cin.
    cout<<x*x;
}