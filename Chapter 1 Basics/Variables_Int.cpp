#include <iostream>
using namespace std;
int main ()
{
    int x=5,y=9; // declaration. We can separate variables by comma (,).
    //Int is like greatest integer function [].
    cout<<x*y<<endl;
    x=1.7; // This is overwriting due to endl command. we can'y change type of variable.
x=x+6; //Here = doesn't mean equality. It means x should be 6 more than previous assigned value of x.
x+=2 ; //this menas add 2 to the previous value of x.
cout<<x;
}