#include <iostream>
using namespace std;
int main ()
{
    int x;
    cout<<"Enter any number : ";
    cin>>x;
    if (x>0) cout<<x;
    else cout<<-x;
}
// == means equal to
// != means not equal to
// > means greater than 
// < means less than 
// >= and <= means ... [They all are called relational operators.]